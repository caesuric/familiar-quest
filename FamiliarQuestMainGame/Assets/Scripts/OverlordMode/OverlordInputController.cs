using UnityEngine;
using System.Collections.Generic;
using System;

public class OverlordInputController : MonoBehaviour {
    private static List<Action> abilities;
    private static Dictionary<string, Action> buttons = new Dictionary<string, Action>();

    // Use this for initialization
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
